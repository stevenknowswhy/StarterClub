import React from "react";
import { HostEventPage } from "@/components/events/host/HostEventPage";

export const metadata = {
    title: "Host a Live Event - Starter Club",
    description: "Host a live event for founders who are ready to decide. Turn your expertise into authority.",
};

export default function Page() {
    return <HostEventPage />;
}
